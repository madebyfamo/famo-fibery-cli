#!/usr/bin/env bash
# Example .envrc file
# Add your environment variables here
# export NODE_ENV=development
# export API_KEY=your_api_key_here

# If you're using nvm for Node.js version management:
# use node 18

# If you need to add the local node_modules/.bin to PATH:
PATH_add node_modules/.bin